# 2026-08-07 工作日志

## 翻译补齐（dashboard 其他页面扫描 + 修复）
- 读透 i18n 引擎（src/i18n/runtime.js）：translate 只处理 DOM 文本节点（SHOW_TEXT 遍历），跳过 table/td/tr/select/code/script 内文本；placeholder/title/aria-label 等属性**不翻译**；confirm/alert 弹窗文字不被 MutationObserver 翻译。
- 引擎 fetch /i18n/literals/<locale>.json，en 直接返回原文；translate(text) 用 trim 后原文作 key 查询，key 必须与渲染后 DOM 文本完全一致（`&apos;` → `'`）。
- 修订 zh-CN.json（+28 行）：补充 25 个新增词条，含：已用 `translate("...")` 包裹但字典缺失的（BulkImportCodexModal 7 条 + providers/[id] 7 条，含 "(inactive)" → （未启用））；以及 proxy-pools 页 Deno/Vercel 特性说明 `<li>` 文本节点（&apos; 需转单引号）。
- 排查方法沉淀：分 3 层扫描（①全量 jsx/字面量→噪音多；②仅 JSX 文本节点 `>text<`；③仅 `translate("...")` 调用比对）。最可靠是第③层。
- `first model` 仍嵌在 `Auto — ${...}` 模板，单独 key 不命中，需改组件（未处理，同前）。
- zh-TW.json 仅 195 key，与 zh-CN（1420 key）不同步，是另一套精简字典（未处理）。