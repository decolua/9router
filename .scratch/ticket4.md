Part of #8
Blocked by: #10

## Question

How should 9router settings accept a persistent Cloudflare Tunnel token (`CLOUDFLARE_TUNNEL_TOKEN`) to invoke `cloudflared tunnel run --token <token>`, establishing a permanent sub-second connection to a dedicated domain without ephemeral `trycloudflare.com` random URLs or slow DNS propagation?
