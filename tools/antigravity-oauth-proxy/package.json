{
  "name": "antigravity-oauth-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Consent-based local OAuth proxy with an OpenAI-compatible chat endpoint.",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
