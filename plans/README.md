# Implementation Plans

Generated by the improve skill on 2026-06-19. Audit of the `bloodf/9router` working tree (HEAD `1271db0`, dirty: 7 modified + 1 untracked). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

> **NOTE (updated 2026-06-22):** Status reconciled against git log + focused-test validation. **001–010 are ALL DONE (validated passing).** 011 and 012 are **PARTIAL** (key work landed; large phases remain — see their rows). Git log + plan **013** are the source of truth (HEAD moves — trust `git log` over any hash). Drive the autonomous run from **013 — DurinDoor Master Execution Plan** (`013-durindoor-master-execution-plan.md`).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Exclude `.worktrees/` from test runs | P1 | S | — | ✅ DONE (`5d3a901`; validated — vitest excludes worktrees) |
| 002  | Guard SSRF in `/api/headroom/probe` | P1 | S | — | ✅ DONE (`66c36dc`; `headroom-ssrf-guard` 16 PASS) |
| 003  | Restrict `?reveal=1` to local requests | P1 | S | — | ✅ DONE (`ff5fc5f`) |
| 004  | Bound `ROUNDROBIN_CURSORS` Map with eviction | P3 | S | — | ✅ DONE (`a04c103`; `modelFallback` 31 PASS). *(The `wave014-roundrobin-bound` worktree was mislabeled cursor-auto-import pollution — discarded; plan 004 was already done.)* |
| 005  | Fix stale `open-sse/AGENTS.md` after engine refactor | P2 | S | — | ✅ DONE (`c00881d`) |
| 006  | Reorder `passesConnectionFilter` after `getProviderStats` | P3 | S | — | ✅ DONE — reorder (`a04c103`) **+ extraction** (T1.1, `93d34c3`; `connectionFilter.test` 28 PASS, parity-reviewed) |
| 007  | Add sync-assertion test for `localDb.js` shim | P2 | S | 001 | ✅ DONE (`localdb-shim-sync` 2 PASS) |
| 008  | Add tests for new untested routes | P2 | M | 001, 002, 003 | ✅ DONE (`reauth-route` 4 + `usage-quota-lock` 10 + `v1-models-codex-caps` 2 PASS) |
| 009  | Add CI workflow for tests and lint | P1 | M | 001 | ✅ DONE (`.github/workflows/ci.yml` present) |
| 010  | Refactor `usage/[connectionId]/route.js` — separate concerns | P3 | L | 001, 008 | ✅ DONE (`a04c103`; route imports `authCheck.js`+`quotaPersist.js`; `usage-quota-lock` 10 PASS covers it) |
| 011  | Upstream sync, latest 10 PR adoption, production hardening, TypeScript migration, LoggerJS watchers | P0 | XL | — | 🟡 **PARTIAL** — Phase 3 upstream **verified DONE** (all 10 candidate PRs already present); production hardening substantially done (CH-02 cli-tools hotspot clean, baseline tight 32==32, dep-security **0 vulns**). **REMAINING: Phase 5 strict-TS (months, dominant cost) + Phase 6 LoggerJS (gated behind 5).** Also deferred: ~65 scattered CH-02 react-hooks, 213 `import/no-anonymous-default-export`. |
| 012  | DurinDoor rebrand, docs, website, logo/favicon, LOTR-inspired theme | P0 | XL | 011 Phase 0 inventory + UI-01 | 🟡 **PARTIAL** — Phase 4 **core DONE** (BRAND.md; metadata→`durindoor`; data-dir unify `~/.durindoor` w/ non-destructive `.9router` back-compat) + **docs DONE** (README + 4 i18n) + **4B service-units DONE** (`404b8f8`; token salt preserved, CLI sessions kept). **REMAINING: 4C provider-config-IDs, UI strings (`sk_9router`, `status.has9Router`), theme tokens; 4D protocol headers DEFERRED (upstream provider coordination).** |
| 013  | DurinDoor Master Execution Plan (autonomous, never-stop) | P0 | XL | supersedes status column; controls 001–012 | 🟢 ACTIVE — controlling plan; drive from here |
| 014  | Kimi (Kimi Code / coding-plan) quota tracker — surface remaining quota/reset/rate-limit in the usage dashboard | P2 | M | — | ⏭ **SKIPPED — user decision (2026-06-22).** Web research found NO documented authenticated endpoint for Kimi *coding-plan* quota (Moonshot `/v1/users/me/balance` is the wrong product). User directive: "if there is no Kimi tracker available documented we skip it." Retained as a research record (`014-kimi-quota-tracker.md`); do NOT reopen unless Kimi publishes a coding-plan quota API. |
| 015  | Seamless 9router→DurinDoor migration (100% back-compatible) — install extracts + replaces an old 9router install with zero manual steps | P0 | L | supersedes 4A "drop 9router bin" + 4B "manual service reinstall" | 🔴 **DESIGN COMPLETE — implement next (user directive 2026-06-22).** Unified `migrateLegacy9router()` triggered at **postinstall** (`npm i -g durindoor`): migrate data dir + runtime + detect/migrate old systemd/launchd service + autostart; back-compat READs (re-add `9router` bin redirect; `[providers.9router]`/`custom:9Router`/`sk_9router` aliases; env done). Reverses 4A drop + 4B manual reinstall. See `015-seamless-9router-migration.md`. |

## Dependency notes

- **001 → 007, 008, 009, 010**: All test-dependent plans need clean test signal first. Plan 001 fixes the `.worktrees/` pollution that makes test output unreliable. Execute 001 before anything that writes or runs tests.
- **002, 003 → 008**: Plan 008 writes tests for the headroom probe and MCP key reveal routes. Plans 002 and 003 fix the security issues in those routes first, so the tests characterize the fixed behavior.
- **001, 008 → 010**: The usage route refactor (plan 010) is the highest-risk plan. It requires clean tests (001) and route-level characterization tests (008) as a safety net before refactoring.
- **001, 002, 003, 005, 006, 007 are independent** — can be executed in parallel by different executors.
- **011 is the umbrella roadmap** for upstream sync, latest upstream PR adoption, production hardening, strict TypeScript migration, and the post-TypeScript LoggerJS log-watcher phase. Execute it in phases after current stabilization branches are reviewed or explicitly folded into the roadmap.
- **012 is the product/brand roadmap** for the DurinDoor rename and LOTR-inspired retheme. Coordinate it with UI-01 and keep official LOTR/Tolkien assets out unless legally approved.
- **013 supersedes the Status column** and orchestrates the entire autonomous run (Phases 0–6) using the model→actor mapping defined inside it.

## Suggested parallelization wave

- **Wave 1** (no deps): 001, 002, 003, 004, 005, 006 — all independent, different files. Safe to dispatch 6 executors.
- **Wave 2** (after 001): 007, 009 — test infra plans.
- **Wave 3** (after 002+003): 008 — route tests.
- **Wave 4** (after 008): 010 — refactor.
- **Roadmap wave**: 011 — use as the controlling plan for upstream sync, PR adoption, remaining production-hardening work, strict TypeScript migration, and the final LoggerJS watcher phase.
- **Brand wave**: 012 — run inventory/brand-system first, then docs/assets/theme in small PRs coordinated with UI-01.
- **Master wave**: 013 — drives Phases 0–6 end to end; follow its model routing and operating loop.

## Findings considered and rejected

- **`connection-proxy` honoring `https_proxy`**: not SSRF — standard proxy convention, by design.
- **Fork/upstream file-level divergence**: documented decision; compare at feature level, not file level.
- **Custom-model capability override Map**: documented decision; hydration is correct.
- **Dual `strategy`/`mode` persistence in fallback config**: deliberate back-compat; engine reads both.
- **EndpointPageClient.js monolith (1488 lines)**: recently trimmed; further split is low-leverage and high-risk (deeply coupled tunnel/status state).
